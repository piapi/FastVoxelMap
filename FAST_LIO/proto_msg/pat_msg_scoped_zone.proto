syntax = "proto3";
package pat.protocol;

message ScopedZoneBegin {
  // 与SrcLocationData中的id相等
  uint64 id = 1;
  int64  time = 2;
  // 用户自定义文本信息
  string txt = 3;
}

message ScopedZoneEnd {
  // 与SrcLocationData中的id相等
  uint64 id = 1;
  int64  time = 2;
}

